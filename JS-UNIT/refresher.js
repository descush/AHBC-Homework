const firstName = "Desiree";
const lastName = "Cushing";

const fullName = `${firstName} ${lastName}`;
console.log(fullName);

const population = 632464
console.log(population);

const threeXpopulation = population * 3;
console.log(threeXpopulation);

let javaScriptIsCool = true;

const colors = ["red", "orange", "yellow", "green", "blue", "purple"];

// Loops: keyword, condition, code
// we can re-use i even though we've declared it twice
// i can be any variable you want but i stands for index

for (let i = 1; i<=5; i++) {
    console.log(i);
}
for (let i = 0; i<=4; i++) {
    console.log(i);
}